<Page x:Class="EF_CORE.Pages.InterestList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EF_CORE.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{Binding RelativeSource={RelativeSource Self}}"
      Title="InterestList">

    <StackPanel>

        <StackPanel Orientation="Horizontal">
            <Button Click="back">Назад</Button>
            <Button Click="add">Создать</Button>
            <Button Click="edit">Изменить</Button>
            <Button Click="remove">Удалить</Button>
        </StackPanel>
        <ListView
             ItemsSource="{Binding service.InterestGroups}"
             MouseDoubleClick="view"
             SelectedItem="{Binding current}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock><Run Text="ID: "/>
                            <Run Text="{Binding Id}"/>
                        </TextBlock>

                        <TextBlock>
                            <Run Text="Группа: "/>
                            <Run Text="{Binding Title}"/>
                        </TextBlock>
                        <TextBlock>
                            <Run Text="Описание: "/>
                            <Run Text="{Binding Description}"/>
                        </TextBlock>
                    </StackPanel>
                    
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
